/**
 *   author:  josuerom
 *   created: 18/02/23 13:50:35
**/
#include <bits/stdc++.h>
using namespace std;

int main() {
   ios::sync_with_stdio(false); cin.tie(0);
   int N;
   cin >> N;
   int contests[N];
   for (auto &e : contests) cin >> e;
   if (N==1 || N==2) cout << (N==1 ? "0" : "1") << '\n';
   else {
      int ans = 1;
      for (int i = 2; i < n; i++) {
         if ()
      }
   }
   return 0;
}